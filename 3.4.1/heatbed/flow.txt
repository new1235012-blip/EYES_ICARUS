graph TD
    Start((Bắt đầu process_gcode)) --> Init[Khởi tạo layers = [], current_e = 0.0]
    Init --> OpenFile[Mở file G-code]
    OpenFile --> LoopStart{Đọc từng dòng (Line-by-line)}
    
    LoopStart -- Hết file --> EndLoop1(Kết thúc vòng lặp đọc file)
    LoopStart -- Còn dòng --> Clean[Cắt khoảng trắng, bỏ dòng trống]
    
    Clean --> CheckLayer{Dòng chứa '; layer' và 'Z =' ?}
    
    %% NHÁNH XỬ LÝ CHUYỂN LAYER
    CheckLayer -- YES --> SaveOld[Lưu current_layer cũ vào list 'layers']
    SaveOld --> ParseZ[Đọc số Z và Layer ID từ dòng đó]
    ParseZ --> ResetNew[Reset current_layer mới\n(Min/Max về vô cực)]
    ResetNew --> LoopStart
    
    %% NHÁNH XỬ LÝ G92
    CheckLayer -- NO --> CheckG92{Dòng bắt đầu bằng 'G92' ?}
    CheckG92 -- YES --> UpdateE_Reset[Cập nhật current_e theo giá trị mới]
    UpdateE_Reset --> LoopStart
    
    %% NHÁNH XỬ LÝ G1 (IN 3D)
    CheckG92 -- NO --> CheckG1{Dòng bắt đầu bằng 'G1' ?}
    CheckG1 -- NO --> LoopStart
    CheckG1 -- YES --> ParseG1[Tách lấy giá trị X, Y, E]
    
    ParseG1 --> CheckExtrude{E mới > current_e ?\n(Có đùn nhựa không)}
    
    CheckExtrude -- NO (Rút nhựa/Travel) --> UpdateE_Only[Chỉ cập nhật current_e]
    UpdateE_Only --> LoopStart
    
    CheckExtrude -- YES (Đang in) --> UpdateBBox[Cập nhật Min/Max X, Y\ncho current_layer]
    UpdateBBox --> UpdateE_Print[Cập nhật current_e]
    UpdateE_Print --> LoopStart
graph TD
    Start2(Bắt đầu tính toán nhiệt) --> CheckLast[Lưu nốt current_layer cuối cùng vào list]
    CheckLast --> InitLogic[Khởi tạo schedule = []\naccumulated_pads = set()]
    
    InitLogic --> LoopLayers{Duyệt từng Layer trong list}
    
    LoopLayers -- Hết Layer --> Return[TRẢ VỀ KẾT QUẢ (Return schedule)]
    
    LoopLayers -- Có Layer --> GetCoords[Lấy Min_X, Max_X, Min_Y, Max_Y]
    
    GetCoords --> CalcMargin[Áp dụng Safe Margin:\nX mở rộng, Y mở rộng]
    
    CalcMargin --> MapGrid[Hàm _get_pad_indices:\nChuyển tọa độ mm -> Danh sách Pad (Row, Col)]
    
    MapGrid --> UnionLogic[LOGIC CỘNG DỒN:\naccumulated_pads = Pad Cũ + Pad Mới]
    
    UnionLogic --> SaveStep[Lưu trạng thái vào schedule:\n{z: ..., active_pads: ...}]
    
    SaveStep --> LoopLayers